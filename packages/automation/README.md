# Bennu Automation

Workflow engine for event-driven business and infrastructure automation.

Example flow:

```text
trigger: new_customer
 -> analyze_profile
 -> generate_proposal
 -> request_approval
 -> send_quote
 -> follow_up
 -> onboarding
```
